# Docker will handle char escaping. If you use 'something' then it will cause problems.
PORT=3000
# AZ app client id
CLIENT_ID=xxxx
# AZ app secret
CLIENT_SECRET=xxxx
# One of: development, debug, production 
NODE_ENV=debug